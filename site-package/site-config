# My site

# Server configuration
# Name of current running instance
server_name = My site

# Port to bind
server_port = 80

# Number of workers
spawn_workers = 8

# Timeout for TCP connections
tcp_timeout = 10

# dir
default_dir = site-package

# Default pages
default_index_page = /index
default_404_page = /404
default_500_page = /500
default_501_page = /501
